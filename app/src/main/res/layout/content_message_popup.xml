<?xml version="1.0" encoding="utf-8"?>
<FrameLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tool="http://schemas.android.com/tools"
    android:layout_width="wrap_content"
    android:layout_height="wrap_content"
    android:background="@color/transparent"
    android:paddingBottom="4dp"
    android:paddingEnd="16dp"
    android:paddingStart="16dp">

    <FrameLayout
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:background="@drawable/shape_message_popup_wrapper"
        android:padding="4dp">

        <LinearLayout
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:orientation="horizontal">

            <ImageView
                android:id="@+id/image_view_edit"
                android:layout_width="40dp"
                android:layout_height="40dp"
                android:padding="8dp"
                android:layout_marginEnd="16dp"
                android:background="@drawable/shape_message_popup"
                app:srcCompat="@drawable/ic_edit_grey_24dp"
                tool:ignore="ContentDescription"/>

            <ImageView
                android:id="@+id/image_view_remove"
                android:layout_width="40dp"
                android:layout_height="40dp"
                android:padding="8dp"
                android:background="@drawable/shape_message_popup"
                app:srcCompat="@drawable/ic_close_grey_24dp"
                tool:ignore="ContentDescription"/>
        </LinearLayout>
    </FrameLayout>
</FrameLayout>